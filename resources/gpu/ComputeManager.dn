uses data.String
uses gpu.Compute

interface ComputeManager {
    ComputeManager ComputeManager(store Compute nativeCompute)
    Compute getNative()
	void findPlatforms()
    String[] getComputeDevices()
    int[] getComputeDeviceIDs()
    void plugInDevice(int deviceID)
    void unplugDevice(int deviceID)
}
